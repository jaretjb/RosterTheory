# TX-002 — Full Roster Theory wordmark and command mastheads

Completed September 16, 2026 (America/Los_Angeles).

The interactive hero now spells out `ROSTER THEORY` in a custom block
wordmark. The completed arcade amendment renders both words as equally large,
stacked pixel lettering inside a bright-yellow 1980s scoreboard frame.
Standard, compact, narrow, and ASCII-safe variants retain the full name. Bare
launch, guided help, and Doctor use the hero; routine human commands and
command-specific help use a smaller arcade-style masthead exactly once. The
old `RT`-only identity is no longer used by CLI reports.

The README SVG extends the console identity into an oversized, chunky
1990s-style game marquee with crisp gold extrusion. Its two title rows are
tightly balanced and centered, and `FANTASY FOOTBALL ASSISTANT` now appears
directly above the Draft/Trade/Waiver mode line. It contains no `read-only`
marketing tagline. Operational documentation still states the actual
read-only Sleeper boundary where it matters.

The console hero now mirrors that hierarchy at every interactive width:
`FANTASY FOOTBALL ASSISTANT` appears immediately above the
Draft/Trade/Waiver mode line beneath the block title. Narrow and ASCII tiers
retain the same words without relying on decorative glyphs. Redirected and
machine output remain unchanged.

Interactive and redirected snapshots pass as part of the complete 528-test
suite. Machine output remains free of art and ANSI.
