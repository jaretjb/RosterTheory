# Trade TA-1308 — provisional read-only rollout complete

The user authorized versioned starting assumptions in place of an unsupported
historical-calibration claim. The target-first finder now builds optional
recent-performance context from compatible completed outcomes and pregame
league-scored captures in a private league/season/scoring-specific history.
It prospectively saves current-week expectations, rejects late captures and
incompatible imports, and leaves missing or small samples unavailable. Expert
rank order and intrinsic trade value do not change because of the residual.

Two separate ignored league-local policies load as `PROVISIONAL_HEURISTIC`.
Both independently start with a three-week window, two-game minimum,
three-game shrinkage prior, 5% direct-chart 2-for-1 premium, and bounded
lane/size search budgets. These are testable guesses, not transferred league
calibration. Each displayed consolidation offer exposes 0/5/10% market
fairness sensitivity; `ECR-PROXY` makes no chart-premium claim. Hashed JSON,
flat CSV, and human output retain the policy status and available performance
evidence. A feedback CSV beside each result can record the manager's
assessment and any actual response without being overwritten or fitted as an
acceptance probability.

Eight prior calibration-harness tests plus new performance-history, workflow,
premium-sensitivity, and feedback checks pass. The full 643-test suite and
Ruff pass. No live finder, Sleeper write, FantasyPros key exposure, or
empirical threshold promotion occurred. The provider/chart and independent
fairness limitations remain visible. The private data audit and future study
contract are in `docs/TRADE_ASSISTANT_TA_1308_CALIBRATION.md`.

TA-1309 owns separately authorized fresh read-only validation for each league.
TA-1310 owns empirical rolling-origin threshold/premium promotion when enough
dated league-local evidence exists. Until then, these outputs are provisional
decision support, not validated trade recommendations.
